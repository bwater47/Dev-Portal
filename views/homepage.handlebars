<section>
  <h1>Homepage</h1>
  {{#each postArray}}
    <div>
      <h2><a href='/post/{{id}}'>{{title}}</a></h2>
      <p>Posted on: {{format_date}}</p>
      <!-- Display comments for the post -->
      {{#each comments}}
        <p>{{this}}</p>
      {{/each}}
    </div>
  {{/each}}
</section>
